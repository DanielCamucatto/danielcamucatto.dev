# Vite Base Path Configuration
# 
# Para desenvolvimento local: use base '/' (padrão)
# VITE_BASE_PATH=/
#
# Para GitHub Pages (project page): use '/danielcamucatto.dev/'
# VITE_BASE_PATH=/danielcamucatto.dev/
#
# Para Netlify: não defina nada (auto-detecta NETLIFY=true)
# O Netlify injeta automaticamente a variável NETLIFY=true durante o build
